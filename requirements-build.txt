# BUILD dependencies (in addition to requirements.txt, which lists the runtime).
#   ./venv/bin/pip install -r requirements.txt -r requirements-build.txt
#
# PyInstaller (pinned to the validated version). Pulls in pyinstaller-hooks-contrib,
# which provides the tkinter/customtkinter/cryptography/lxml/PIL/certifi hooks.
pyinstaller==6.20.0

# tzdata: ONLY on Windows. pyHanko timestamps the signature via tzlocal,
# which builds a zoneinfo.ZoneInfo there; since Windows has no system zoneinfo
# database, the tzdata package must be bundled. On Linux/macOS, the system
# database is enough (environment marker -> pip ignores it outside Windows).
tzdata; sys_platform == "win32"
