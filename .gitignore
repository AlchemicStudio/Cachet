# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# --- Virtualenv (provisioned locally; not committed) ---
venv/

# --- PyInstaller build artifacts ---
build/
dist/
*.manifest
# Binary transfer archives (e.g. dist.zip to a Windows PC) — too large for git
# (GitHub's 100 MB limit); transfer them outside the repository.
*.zip

# --- Wine cross-build: prefix, downloaded Python installer, fixtures ---
wine-prefix/
.wine-cachet/
python-*-amd64.exe
wine_in.pdf
wine_sig.png
wine_fake.dll
wine_out/

# --- Misc ---
*.log
.DS_Store
